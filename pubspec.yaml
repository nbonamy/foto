---
name: foto
description: A new Flutter project.
publish_to: 'none'  # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

platforms:
  macos:

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  contextual_menu:
    git:
      url: https://github.com/nbonamy/contextual_menu
  cupertino_icons: ^1.0.9
  exif: ^3.2.1
  filesize: ^2.0.1
  image_size_getter: ^2.4.1
  image_size_getter_heic:
    git:
      url: https://github.com/nbonamy/dart_image_size_getter
      path: image_size_getter_heic
  intl: ^0.20.2
  pasteboard: ^0.5.0
  path: ^1.9.1
  photo_view: ^0.15.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  window_manager: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/img/
    - assets/img/folders/
    # - assets/i18n/
